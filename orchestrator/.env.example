# Health check only — all AI endpoints (Voice-to-Chart, Notes, Prescription,
# Treatment Plan) have migrated to Supabase Edge Functions (see
# supabase/functions/, `supabase secrets` for their GROQ_API_KEY etc).
# WhatsApp/Instagram/Facebook + AI Receptionist vars will be added here if
# the orchestrator is kept for that future non-AI work — see DentVerseDocs/12-milestones.

PORT=3001

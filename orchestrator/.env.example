# Milestone 0 — health check only; none of these are consumed yet.
# Wired up starting Milestone 4 (WhatsApp channel plumbing) and
# Milestone 5 (AI Receptionist). See DentVerseDocs/12-milestones.

PORT=3001

# Supabase — service-role key bypasses RLS deliberately; the
# orchestrator is the trust boundary for patient-originated writes.
# See DentVerseDocs/02-architecture/system-architecture.md.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
